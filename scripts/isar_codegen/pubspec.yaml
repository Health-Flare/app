name: isar_codegen
description: "Minimal project for generating Isar .g.dart files for healthflare."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.3.0

dependencies:
  isar_community: ^3.3.0

dev_dependencies:
  build_runner: any
  isar_community_generator: ^3.3.0

# No flutter dependency — this is a plain Dart project.
